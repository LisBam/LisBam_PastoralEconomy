package lisbam.pastoraleconomy.transport;

/** Bounded C2S transport GUI actions; all authoritative values are server-derived. */
public enum TransportAction {
    REFRESH,
    CONNECT,
    REMOVE,
    RENAME,
    FIND_VILLAGE,
    CONNECT_VILLAGE,
    TRAVEL
}
