# AGENTS.md

# 聆竹の休闲田园经济 — Codex 项目工作规范

本文件是本项目所有 Codex 任务的长期工作规范。

无论当前是新的 Codex 会话、压缩后的上下文、继续以前的任务，还是换了模型，都必须优先读取并遵守本文件。

本项目采用 **分阶段增量开发**。

整个模组会被拆分为约 16 个制作批次逐步完成。每次只实现当前批次，在保证已有功能稳定的基础上继续扩展。

---

# 1. 项目基本信息

目标游戏：

- Minecraft Java Edition 1.12.2

模组加载器：

- Minecraft Forge 1.12.2

Forge：

- 保持项目当前使用的 Forge `14.23.5.x`
- 不得未经允许升级 Forge

Java：

- Java 8
- 不允许使用 Java 9+ 语法或 API

开发方式：

- Forge 1.12.2 原生模组
- 优先使用项目内 Gradle Wrapper
- 不依赖全局 Gradle

本项目名称：

**聆竹の休闲田园经济**

开发模式：

**按照“Codex分阶段制作”文档逐批实现。**

---

# 2. Forge 1.12.2 Skill

如果当前 Codex 环境安装了：

`minecraft-forge-1-12-2`

Skill，则所有涉及 Minecraft Forge 开发、修改、调试、审查、构建、测试的任务都必须遵守该 Skill。

Skill 中规定的 Forge 1.12.2 API 边界、客户端/服务端原则、持久化原则、注册方式、构建方式和测试要求优先级高于 Codex 根据现代 Minecraft 开发经验进行的推测。

严禁因为模型更熟悉现代 Minecraft，而把现代 Forge API 写入本项目。

尤其禁止误用：

- NeoForge
- Fabric
- `mods.toml`
- `DeferredRegister`
- `RegistryObject`
- `BlockEntity`
- Mojmap 时代名称
- Java 9+ API
- 现代 Forge 注册体系
- 现代网络 API
- 现代数据组件 API

遇到不确定的 Forge 1.12.2 API 时：

**必须查询项目源码、Forge 1.12.2 源码、Gradle 缓存、已有项目实现或 Skill 参考资料。**

不得凭现代 Forge 经验猜测接口。

---

# 3. 项目文件就是长期记忆

不要依赖当前聊天上下文记住整个项目。

项目本身必须保存开发状态。

在开始任何新的开发任务之前，优先读取：

- `AGENTS.md`
- `docs/MOD_ARCHITECTURE.md`
- `docs/DEVELOPMENT_PROGRESS.md`
- `docs/IMPLEMENTATION_DECISIONS.md`
- `docs/TEST_CHECKLIST.md`

如果上述 `docs` 文件不存在，在第一次正式开发时创建。

这些文件属于项目的一部分，需要随着开发持续维护。

---

# 4. 文档职责

## docs/MOD_ARCHITECTURE.md

记录当前模组真实的软件架构。

至少记录：

- Mod 主入口
- 包结构
- 注册系统
- 事件系统
- 网络系统
- GUI 系统
- 数据保存系统
- Capability
- WorldSavedData
- TileEntity
- Entity
- 玩家数据
- 世界数据
- 经济系统
- 商人系统
- 市场价格系统
- 农业系统
- 附魔系统
- 村庄系统
- 交通系统
- 配置系统
- 客户端专用模块
- 服务端权威模块
- 各系统之间的依赖关系

只记录**实际存在的架构**。

不要把尚未实现的策划内容描述成已经实现。

如果当前批次改变了系统架构，必须同步更新此文件。

---

## docs/DEVELOPMENT_PROGRESS.md

这是整个 16 批开发流程的主要进度记录。

记录：

- 当前已经完成到第几批
- 每一批完成日期
- 每一批实现内容
- 哪些功能已经可用
- 哪些功能仍然只是基础框架
- 哪些功能等待后续批次
- 当前已知问题
- 当前技术债
- 当前未完成测试
- 下一批开始前需要注意的事项

推荐格式：

```markdown
# 当前状态

当前完成批次：第 X 批

最近成功构建：
- ./gradlew build
- 日期：
- 结果：

# 批次记录

## 第 01 批

状态：完成

实现：
- ...

主要文件：
- ...

验证：
- ...

遗留：
- ...

## 第 02 批

状态：完成

...
```

不要删除以前的批次历史。

---

## docs/IMPLEMENTATION_DECISIONS.md

记录重要技术决策以及做出这些决策的原因。

例如：

```markdown
## DEC-001 玩家货币存储

决定：

玩家货币使用 Capability 保存。

原因：

货币属于玩家长期状态，需要：
- 保存
- 死亡处理
- 克隆处理
- 服务端权威
- 网络同步

影响系统：

- 商人
- 市场
- 交通
- 后续经济功能
```

适合记录：

- 为什么选择 Capability
- 为什么选择 WorldSavedData
- 哪个系统负责价格
- 哪个系统负责货币
- 谁拥有某项数据
- 客户端与服务端如何同步
- 网络包为什么这样划分
- 注册名称为什么这样定义
- 保存格式为什么这样定义
- 为后续批次预留了什么接口

如果以后修改以前的重要技术决策：

不要直接删除旧记录。

应该说明：

- 原决定
- 修改原因
- 新决定
- 数据兼容影响

---

## docs/TEST_CHECKLIST.md

记录整个模组持续使用的测试清单。

每增加一个系统，应增加相应测试项。

至少长期检查：

### 构建

- [ ] `compileJava`
- [ ] `processResources`
- [ ] `build`

### 游戏启动

- [ ] Forge 正常加载
- [ ] 模组正常识别
- [ ] 无严重启动错误
- [ ] 无缺失注册项
- [ ] 无明显资源错误

### 单人游戏

- [ ] 创建新世界
- [ ] 保存世界
- [ ] 退出世界
- [ ] 重新进入世界
- [ ] 玩家死亡
- [ ] 玩家重生
- [ ] 切换维度

### 数据

- [ ] 玩家数据保存正确
- [ ] 世界数据保存正确
- [ ] 游戏重启后数据存在
- [ ] 数据不会重复初始化

### 客户端 / 服务端

- [ ] 游戏状态由逻辑服务端负责
- [ ] 客户端不能直接修改权威数据
- [ ] 网络包经过服务端验证
- [ ] 网络处理线程安全

### Dedicated Server

涉及以下内容时必须额外检查：

- 网络
- Entity
- Capability
- WorldSavedData
- GUI / Container
- 世界事件
- 玩家事件
- 命令
- Tick
- 服务端数据

检查：

- [ ] Dedicated Server 可以启动
- [ ] Common 代码没有加载客户端专用类

### 回归测试

每完成一个新批次：

- [ ] 当前批次功能正常
- [ ] 以前批次核心功能仍然正常

---

# 5. 每次开始任务前必须执行项目检查

任何代码修改之前，先了解项目当前状态。

至少检查：

```bash
pwd
git status --short
```

并检查：

- `build.gradle`
- Gradle Wrapper
- Forge 版本
- mappings
- Java 版本
- modid
- package
- 当前源码结构
- 当前资源目录
- 当前 AGENTS.md
- docs 中的项目记录

不要假设这些信息和上一个 Codex 会话完全一致。

**磁盘上的当前项目状态才是真实状态。**

如果 Skill 提供 Forge 1.12.2 项目检查脚本，则应使用。

---

# 6. 分批开发原则

本模组采用严格的分批开发。

通常一次任务会提供：

1. `Codex分阶段制作_第XX批`
2. 当前批次对应的实施任务单

两者共同构成本批需求。

其中：

### 分阶段制作文档

负责规定：

**要做什么。**

包括：

- 功能
- 规则
- 游戏行为
- 数值
- 物品
- 方块
- GUI
- 配方
- 经济规则
- 玩法规则

其中明确给出的具体数值属于正式设计要求。

不得擅自修改。

### 实施任务单

负责规定：

**这一批应该如何实现。**

包括：

- 技术路线
- 系统边界
- 数据所有权
- 客户端/服务端职责
- 文件结构
- 实现顺序
- 接口预留
- 验收要求

除非实施任务单与实际 Forge 1.12.2 API 明显冲突，否则应遵守。

如果两份文档存在真正的逻辑冲突：

优先保证：

1. 用户明确要求
2. 游戏设计的具体规则和数值
3. Forge 1.12.2 正确性
4. 项目现有架构一致性
5. 实施任务单推荐方案

不要因为实现方便而偷偷改变玩法。

---

# 7. 一次只完成当前批次

默认情况下：

**不要主动实现后续批次的完整功能。**

例如：

如果当前是第 3 批，而第 7 批会加入完整市场系统：

可以：

- 为以后市场系统留下合理接口
- 保持架构可扩展
- 避免未来明显需要推翻当前代码

但不要：

- 提前完整实现第 7 批
- 自己设计尚未要求的玩法
- 添加大量推测性的系统
- 添加未要求的依赖

原则：

**为未来留接口，但不要提前制造未来。**

---

# 8. 必须保护以前已经完成的功能

开始新批次之前，应先理解已有代码。

优先寻找项目中已经存在的实现模式并复用。

不要每一批重新设计一个新架构。

如果已经存在：

- Registry 管理器
- PacketHandler
- Capability
- WorldSavedData
- GUI Handler
- Config
- CommonProxy
- ClientProxy
- 数据服务
- 经济服务
- 工具类

优先扩展已有系统。

不要无意义创建：

- 第二套网络框架
- 第二套经济框架
- 第二套保存框架
- 重复 Manager
- 重复 Helper
- 重复 Registry

---

# 9. 数据所有权必须明确

任何新增状态都必须回答：

**这份数据真正属于谁？**

通常分为：

### 玩家数据

例如：

- 玩家货币
- 玩家解锁状态
- 玩家个人统计

应选择适合玩家生命周期的保存方式。

### 世界数据

例如：

- 全局市场状态
- 世界级经济数据
- 村庄信息
- 全局线路信息

应选择适合世界持久化的保存方式。

### 方块实例数据

例如：

- 某个交通方块自身状态

根据实际需求考虑 TileEntity。

### 临时客户端状态

例如：

- 当前 GUI 页面
- 临时动画
- 非权威显示缓存

可以只存在客户端。

不得把重要游戏状态只保存在 GUI 或客户端字段里。

---

# 10. 服务端权威

所有会影响游戏结果的重要数据默认由逻辑服务端负责。

尤其包括：

- 钱
- 买卖
- 市场价格
- 商品数量
- 经验
- 掉落物
- 附魔触发
- 交通扣费
- 解锁
- 村庄状态
- 世界数据
- 玩家持久化数据

客户端负责：

- 输入
- GUI
- 显示
- 动画
- 请求

客户端发送请求后：

**服务端必须重新验证。**

例如购买操作不能只是：

```text
客户端说：“我已经扣了100金币，请给我物品。”
```

而应该是：

```text
客户端：“我请求购买这个商品。”

服务端：
1. 检查商品是否合法
2. 检查当前价格
3. 检查玩家余额
4. 检查购买条件
5. 扣除货币
6. 发放商品
7. 保存状态
8. 同步客户端
```

不要信任客户端提交的重要数值。

---

# 11. 注册 ID 和存档兼容

已经正式使用的注册 ID 不要随意修改。

包括：

- Block RegistryName
- Item RegistryName
- Entity ID / ResourceLocation
- TileEntity ID
- Enchantment RegistryName
- 网络相关稳定标识
- NBT key
- WorldSavedData 名称

如果确实需要修改：

首先评估：

- 是否破坏旧存档
- 是否导致物品消失
- 是否导致 Missing Mapping
- 是否导致数据无法读取

不要为了“名字更漂亮”破坏存档兼容。

---

# 12. 不要大规模无关重构

每一批开发以：

**最小完整垂直切片**

为原则。

完成当前功能所必要的：

- Java
- 注册
- resources
- lang
- models
- blockstates
- recipe
- 网络
- 保存
- GUI
- 测试

应该一起完成。

但是不要顺便：

- 重写整个项目
- 大规模格式化无关文件
- 重命名大量类
- 移动整个 package
- 重写已经稳定的基础架构

除非当前任务明确要求。

---

# 13. 不得破坏用户已有修改

修改前检查：

```bash
git status --short
```

如果存在用户未提交修改：

必须保留。

不要：

- `git reset --hard`
- 随意 restore 用户文件
- checkout 覆盖用户内容
- 删除来源不明文件
- 为解决冲突直接抹掉已有代码

只修改完成当前任务真正需要修改的内容。

---

# 14. 资源必须和 Java 一起完成

Forge 模组功能不仅是 Java。

如果新增内容需要：

- lang
- recipe
- model
- blockstate
- texture 引用
- mcmod.info
- sounds.json
- loot table

则必须同步处理。

不能因为 Java 能编译，就认为一个 Item、Block 或 GUI 已经完成。

---

# 15. 每批标准开发流程

每个批次按照以下顺序进行。

## Step 1：读取长期状态

阅读：

- `AGENTS.md`
- `MOD_ARCHITECTURE.md`
- `DEVELOPMENT_PROGRESS.md`
- `IMPLEMENTATION_DECISIONS.md`
- `TEST_CHECKLIST.md`

理解：

- 已完成什么
- 当前架构是什么
- 有哪些遗留问题
- 后续系统已经预留哪些接口

---

## Step 2：读取本批完整需求

完整阅读当前：

- 分阶段制作文档
- 实施任务单

不要只根据标题开始写代码。

提取：

- 功能
- 精确数值
- 前置条件
- 数据
- 游戏行为
- 异常情况
- 验收条件

---

## Step 3：检查现有实现

搜索整个项目寻找：

- 可复用类
- 已有接口
- 已有 Registry
- 已有 EventHandler
- 已有 Packet
- 已有 Capability
- 已有 WorldSavedData
- 已有 GUI
- 已有 Config

优先扩展。

---

## Step 4：确定实现方案

在真正修改前，在内部明确：

- 哪个类负责什么
- 数据属于谁
- 谁保存
- 谁同步
- 谁验证
- 谁注册
- 客户端负责什么
- 服务端负责什么
- 是否涉及旧存档

避免边写边随机改变架构。

---

## Step 5：实现最小完整功能

完成当前批次所需的完整纵向功能。

不要留下明显的：

- TODO 假实现
- 空方法
- 永远返回固定值
- 只写 GUI 不写后端
- 只写后端不注册
- 只写 Java 不写资源
- 测试用作弊逻辑

除非本批明确规定只是建立基础框架。

---

## Step 6：构建

优先使用：

Linux / WSL：

```bash
./gradlew compileJava
./gradlew processResources
./gradlew build
```

Windows：

```bat
gradlew.bat compileJava
gradlew.bat processResources
gradlew.bat build
```

如果 compileJava 失败：

修复以后重新运行。

不要在存在编译错误的情况下宣布任务完成。

---

## Step 7：Forge 1.12.2 审计

如果 `minecraft-forge-1-12-2` Skill 提供静态审计工具：

运行 Forge 1.12.2 API 审计。

检查：

- 现代 API 泄漏
- Java 版本问题
- 错误 metadata
- 注册问题
- 资源问题
- side 问题

发布或大阶段检查时使用严格模式。

---

## Step 8：运行测试

根据本批修改范围选择：

- `runClient`
- `runServer`

如果修改：

- networking
- persistence
- entity
- world
- command
- common code
- GUI / Container
- Capability
- WorldSavedData

不能仅因为 `runClient` 成功就认为服务端安全。

---

## Step 9：回归检查

检查：

**本批功能是否正确。**

同时检查：

**以前完成的核心功能是否仍然工作。**

不能出现：

“第8批完成，但第3批功能被破坏。”

---

## Step 10：更新长期记忆

任务完成后必须更新：

- `MOD_ARCHITECTURE.md`
- `DEVELOPMENT_PROGRESS.md`
- `IMPLEMENTATION_DECISIONS.md`
- `TEST_CHECKLIST.md`

只更新本次真正发生变化的部分。

---

# 16. 遇到 Bug 时

不要直接用 try/catch 把错误吞掉。

首先找：

- 第一处真正异常
- 最早的 `Caused by`
- 日志中的注册错误
- Side-only class loading
- Packet thread
- NullPointerException
- 数据加载错误
- 资源路径错误
- Forge 生命周期错误

优先修根因。

不要用：

```java
try {
    brokenCode();
} catch (Exception ignored) {
}
```

来把严重问题伪装成正常。

---

# 17. 不确定 API 时

禁止：

**“我记得 Forge 大概是这么写。”**

必须验证。

优先顺序：

1. 当前项目已有实现
2. Forge 1.12.2 Skill
3. 项目 Gradle 缓存中的源码
4. Forge 1.12.2 源码
5. Minecraft 1.12.2 源码 / mappings
6. Forge 1.12.2 文档或 Javadocs

不要套用：

- 1.16
- 1.18
- 1.19
- 1.20
- 1.21+
- NeoForge
- Fabric

教程代码。

---

# 18. 新增抽象的原则

不要为了“架构漂亮”创建大量抽象。

只有满足以下条件时才值得抽象：

- 多个系统已经需要它
- 后续明确会复用
- 能减少重复代码
- 能稳定系统边界

否则优先使用简单实现。

本项目追求：

**可靠、容易维护、容易继续开发。**

不是追求过度工程化。

---

# 19. 后续批次兼容原则

虽然一次只实现当前批次，但必须考虑已经明确存在的后续设计。

例如：

如果货币以后会同时被：

- 商人
- 市场
- 交通
- 任务
- 其他经济玩法

调用，那么货币系统应该提供统一接口。

不要让每个系统自己修改玩家余额。

正确方向：

```text
EconomyService
    ↓
统一负责余额查询 / 增加 / 扣除 / 校验
    ↓
商人 / 市场 / 交通调用
```

这是合理的架构预留。

但是不要因为以后会有市场，就在当前批提前实现完整市场。

---

# 20. 防止做到后期失控

随着批次增加，每次开始任务都必须先确认：

- 当前批次编号
- 已完成批次
- 当前真实项目结构
- 当前核心系统
- 本批依赖哪些以前系统
- 本批会影响哪些以前功能

如果文档和代码不一致：

**代码是“现在实际发生了什么”的事实来源。**

然后修正文档。

如果设计文档明确规定游戏行为，则：

**设计文档是“游戏应该是什么”的需求来源。**

不能因为现有代码写错了，就把错误代码当作设计。

---

# 21. 不允许伪造完成状态

只有真正成功执行过的测试才能写：

`PASS`

如果由于环境限制没有运行：

必须写：

`NOT RUN`

并说明原因。

不要说：

- “理论上可以编译”
- “应该没有问题”
- “看起来可以正常运行”

然后把状态标为完成。

构建结果必须基于真实命令结果。

---

# 22. 每批完成报告格式

每次完成任务后，最终回复尽量保持简洁，使用：

```text
第 XX 批完成

实现：
- ...
- ...

架构：
- ...
- ...

主要修改：
- ...
- ...

验证：
- ./gradlew compileJava — PASS
- ./gradlew processResources — PASS
- ./gradlew build — PASS
- runClient — PASS / NOT RUN
- runServer — PASS / NOT RUN

文档：
- MOD_ARCHITECTURE.md — 已更新
- DEVELOPMENT_PROGRESS.md — 已更新
- IMPLEMENTATION_DECISIONS.md — 已更新
- TEST_CHECKLIST.md — 已更新

遗留：
- 无

下一批注意：
- ...
```

不要输出长篇教程。

重点告诉用户：

- 做了什么
- 为什么这样设计
- 是否构建成功
- 有什么遗留

---

# 23. Bug 修复完成报告

如果本次主要是修 Bug，则报告：

```text
问题原因：
- ...

修复：
- ...

影响：
- ...

验证：
- ...

是否影响存档：
- 否 / 是，说明影响
```

重点解释：

**为什么坏，以及怎么修好的。**

---

# 24. Git 原则

Codex 可以：

- 查看 diff
- 查看 status
- 帮助组织修改

但是：

未经明确要求，不要：

- push
- force push
- reset --hard
- rebase
- 删除 branch
- 修改远程仓库
- 自动覆盖用户提交

如果用户要求提交：

先检查：

```bash
git status
git diff
```

确认修改范围和当前任务一致。

---

# 25. 第 16 批完成后

第 16 批完成不代表项目立即结束。

进入：

# 最终集成验收阶段

完整检查：

## 构建

- compileJava
- processResources
- build

## 客户端

- Minecraft 启动
- Mod 加载
- 新建世界
- 旧世界
- 退出重进
- 游戏重启

## Dedicated Server

- Server 启动
- 玩家连接
- 服务端日志
- Common / Client Side 安全

## 数据

- 玩家数据
- 世界数据
- TileEntity
- Capability
- WorldSavedData
- NBT
- 数据同步

## 游戏行为

逐项检查 16 批实现的主要功能。

## 经济系统

重点检查：

- 金钱凭空增加
- 重复交易
- 负数交易
- 客户端伪造数据
- 重复请求
- 刷钱
- 重复领取
- 物品复制
- 重连异常
- 重启异常

## 兼容

检查：

- 注册 ID
- 世界加载
- 保存
- 重启
- 维度
- 死亡
- 重生

只有最终集成测试通过后，才认为整个模组进入可发布状态。

---

# 26. 核心原则总结

任何时候都遵守以下优先级：

1. 不破坏已有功能
2. 严格遵守 Minecraft Forge 1.12.2
3. 严格遵守当前批次需求和具体数值
4. 服务端负责权威游戏状态
5. 项目文件而不是聊天历史负责长期记忆
6. 一次完成一个批次
7. 为后续留合理接口，但不要提前实现后续完整系统
8. 每批必须构建和验证
9. 每批必须更新项目长期文档
10. 不允许用“应该可以”代替真实测试结果

最终目标不是：

**“让这一批代码暂时能跑。”**

而是：

**让 16 个批次能够连续叠加，最终形成结构稳定、存档可靠、可以继续维护的完整 Forge 1.12.2 模组。**